<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.edu.tecsup.offerplaza">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Home"></activity>
        <activity android:name=".activities.compras" />
        <activity android:name=".activities.empresas" />
        <activity android:name=".activities.Empresa" />
        <activity android:name=".activities.toolbar" />
        <activity
            android:name=".activities.Productos"
            android:parentActivityName=".activities.empresas" />
        <activity android:name=".MainActivity" />
        <activity android:name="activities.DetalleEmpresa" />
        <activity android:name=".activities.VisorImagen" />
        <activity android:name=".activities.Registro" />
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>